import { Link } from 'react-router-dom'

const Recipeater = () => {
  return (
    <div>
      <p>Currently unavailable. No release date decided.</p>
      <Link to='/' className='App-link'>Go Back</Link>
    </div>
  )
};

export default Recipeater;
